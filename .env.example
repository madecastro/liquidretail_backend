# ─────────────────────────────────────────────────────────────────────
# This file documents EVERY env var. Non-secret defaults (feature flags,
# tuning knobs, public IDs/URLs) now live in config/defaults.env (loaded
# in index.js/worker.js AFTER the environment — env always wins). Only the
# SECRETS below (keys/tokens/URIs) belong in the Render environment. See
# docs/PIPELINES.md § Configuration & secrets.
# ─────────────────────────────────────────────────────────────────────
MONGODB_URI=your-mongodb-connection-string

# ── AI gateway (PRIMARY) ─────────────────────────────────────────────
# ALL LLM chat/vision, image generation/editing, and video generation
# route through Atlas Cloud (api.atlascloud.ai) — one key, one ledger.
# Model mapping + gateway client: services/atlasModelMap.js /
# atlasLlmService.js / atlasImageService.js / atlasVideoService.js.
# Full architecture + exception list: docs/ATLAS.md.
ATLAS_API_KEY=your-atlascloud-key

# ── Direct-provider FALLBACK keys (optional but recommended) ─────────
# When set, gateway failures replay against the original provider with
# the original models. Also REQUIRED by the documented exceptions that
# never migrated: mask inpainting (openaiImageService) + whisper
# transcription need OPENAI_API_KEY; Gemini google_search grounding
# (geminiSearchProvider, productDetails, categoryReviews) needs
# GEMINI_API_KEY. See docs/ATLAS.md.
OPENAI_API_KEY=your-openai-key
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
SHOPIFY_API_KEY=your_key
SHOPIFY_API_SECRET=your_secret
SHOPIFY_ACCESS_TOKEN=your_access_token
SHOPIFY_STORE_DOMAIN=yourstore.myshopify.com
RUN_WORKER=true
# How many DetectRuns process concurrently in this worker process.
# Default 2 (safe on Render free tier 512MB). Bump up on paid plans:
#   Standard 2GB → 8-10
#   Pro      4GB → 20-30
#   Pro Plus 8GB → 50+
# Pipeline is mostly I/O so memory is the real ceiling. Cap is 100.
# Mongoose pool sizes itself to 3× concurrency (capped at 200) to
# avoid head-of-line blocking at high concurrency.
WORKER_CONCURRENCY=4   # parallel worker loops (default 4; prod 5). I/O/API-bound.

# Gemini DIRECT key — fallback path + the grounded-search exceptions
# (google_search grounding is not expressible through the Atlas gateway).
# AI Studio key, 39 chars, prefix "AIza..." — https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-ai-studio-key
# Optional: pin a specific model for image generation. If unset, ListModels picks
# the first image-capable one. e.g. gemini-2.5-flash-image, gemini-3.1-flash-image-preview
GEMINI_IMAGE_MODEL=
# Model used for grounded product-match search (text-based). Default gemini-2.5-flash.
GEMINI_SEARCH_MODEL=

# SerpAPI — Google Lens visual product matching (detect pipeline, product-match stage)
# Get from https://serpapi.com/dashboard
SERPAPI_API_KEY=your-serpapi-key
# Optional: country biasing for Google Lens results. Default "us".
SERPAPI_COUNTRY=

# Brandfetch — brand-kit lookup (logo, colors, fonts) by domain. Used by
# brand enrichment as the primary visual-identity source; falls back to
# homepage scraping (apple-touch-icon + Google Fonts links + meta theme-color)
# when missing or 404. Get from https://developers.brandfetch.com/
BRANDFETCH_API_KEY=your-brandfetch-key

# Google OAuth — USER LOGIN client. Should be a SEPARATE OAuth client
# from the Google Ads one below. This one only requests basic scopes
# (email, profile, openid) so the consent screen can be PUBLISHED
# without going through Google's sensitive-scope verification process
# (1–2 week review). With this client published, anyone with a Google
# account can sign in without being added to a Test users allowlist.
#
# Setup (Google Cloud Console):
#   1. APIs & Services → Credentials → Create Credentials → OAuth client ID
#   2. Application type: Web application
#   3. Authorized redirect URIs: <this backend's domain>/auth/google/callback
#   4. APIs & Services → OAuth consent screen → Publishing status → PUBLISH
#      (no verification needed for basic scopes)
GOOGLE_CLIENT_ID=your-google-login-client-id
GOOGLE_CLIENT_SECRET=your-google-login-client-secret
GOOGLE_CALLBACK_URL=https://liquidretail-backend.onrender.com/auth/google/callback
SESSION_SECRET=change-me-to-a-long-random-string
JWT_SECRET=change-me-to-a-long-random-string

# Where the backend bounces users after OAuth (Google signin + IG /
# Meta Ads / Google Ads callbacks land on this domain's hash/query).
# Defaults to https://liquidretail.netlify.app when unset.
# Set on a dev/staging backend to point at the dev frontend domain.
FRONTEND_URL=

# Meta / Instagram OAuth (Phase A — Connect Instagram on brand page).
# Register a Business app at https://developers.facebook.com/apps/, add
# the Facebook Login product, set Valid OAuth Redirect URIs to the
# callback URL below, and request these permissions during App Review:
# instagram_basic, pages_show_list, pages_read_engagement,
# business_management, catalog_management.
META_APP_ID=your-meta-app-id
META_APP_SECRET=your-meta-app-secret
META_REDIRECT_URI=https://liquidretail-backend.onrender.com/api/integrations/instagram/callback
# Optional: pin a specific Graph API version. Default v19.0.
META_API_VERSION=

# AES-256 key for encrypting third-party access tokens at rest. Must be
# 32 bytes base64-encoded. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
INTEGRATION_ENCRYPTION_KEY=base64-32-bytes-here

# Meta webhook verify token (V3 #1). Arbitrary string you also paste
# into the Meta App Dashboard when setting up the webhook subscription;
# Meta echoes it back during the GET verification handshake.
# Generate with:
#   node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
META_WEBHOOK_VERIFY_TOKEN=any-long-random-string

# Meta Ads OAuth callback (Ad Platforms Phase A). Same Meta App as the
# IG integration above, just a separate Valid OAuth Redirect URI for
# ads scopes (ads_read, ads_management, business_management). Add this
# URL to the same Meta App's Facebook Login → Valid OAuth Redirect URIs.
META_ADS_REDIRECT_URI=https://liquidretail-backend.onrender.com/api/integrations/meta-ads/callback

# Google Ads OAuth — SENSITIVE-SCOPE client. Use a SEPARATE OAuth client
# from the login one above. This one requests the adwords scope, which
# Google flags as sensitive — the consent screen cannot be published
# without going through OAuth verification (privacy policy, demo video,
# 1–2 week review). Keep this client in TESTING mode and add the operators
# who will link Google Ads accounts to the Test users allowlist (max 100).
#
# Splitting this from the login client means: login is open to anyone
# (no allowlist), and only the few users who actually link a Google Ads
# account need to be on the Test users allowlist.
#
# Setup (Google Cloud Console — same project as login is fine, or the
# project whose developer-token app is approved for Google Ads):
#   1. APIs & Services → Credentials → Create Credentials → OAuth client ID
#   2. Application type: Web application
#   3. Authorized redirect URIs: <this backend's domain>/api/integrations/google-ads/callback
#   4. OAuth consent screen → leave Publishing status as Testing
#   5. OAuth consent screen → Test users → add operator emails
#
# If GOOGLE_ADS_CLIENT_ID is unset, the service falls back to
# GOOGLE_CLIENT_ID/SECRET above — convenient for single-client dev but
# couples your login users to the sensitive-scope verification gate.
GOOGLE_ADS_CLIENT_ID=
GOOGLE_ADS_CLIENT_SECRET=
GOOGLE_ADS_REDIRECT_URI=https://liquidretail-backend.onrender.com/api/integrations/google-ads/callback

# Google Ads Developer Token. Server-wide secret required by every
# Google Ads API call. Apply at https://ads.google.com/aw/apicenter
# (test access is granted instantly; production requires Google review).
GOOGLE_ADS_DEVELOPER_TOKEN=your-google-ads-developer-token

# Optional: pin the Google Ads API version used by REST calls. Default v17.
GOOGLE_ADS_API_VERSION=

# ── Video generation provider ────────────────────────────────────────
#
# The video pipeline ships with two providers behind videoRouter.js:
#   vertex (default) — Vertex AI Veo 3.1 direct. Uses GEMINI_API_KEY.
#   atlas            — Atlas Cloud, defaulting to Gemini Omni Flash
#                      image-to-video (google/gemini-omni-flash/
#                      image-to-video-developer): Ken Burns camera-only
#                      animation over the supplied product photos,
#                      8s @ 24fps, up to 7 reference images (default 3).
#                      ≈$1.00 per 8s/720p render (see
#                      atlasVideoService.estimateRenderCostUsd; every
#                      render logs a CostLog entry, stage
#                      'atlas_video_render').
#
# Output is motion-only on both providers — all on-screen text is
# composited downstream by the brand-script overlay.
#
# Defaults to atlas (Atlas migration 2026-07-21); set VIDEO_PROVIDER=vertex
# to fall back to the deprecated direct-Veo path.
VIDEO_PROVIDER=atlas

# Atlas Cloud video — uses the primary ATLAS_API_KEY defined at the top.
# Override the Atlas model deployment-wide. Defaults to
# google/gemini-omni-flash/image-to-video-developer. Per-brand and
# per-product overrides (Brand.videoSettings / CatalogProduct.videoSettings
# — flat `model` or per-canvas `modelByCanvas`) take precedence over this
# env; the regenerate dropdown's per-run override beats everything.
# Operator-selectable slugs (also served by GET /api/ads/video-models):
#   google/gemini-omni-flash/image-to-video-developer      (default)
#   google/gemini-omni-flash/reference-to-video-developer  (video-seeded ads)
#   xai/grok-imagine-video-v1.5/image-to-video             (single-frame, 7 aspects)
# Slugs must exist in atlasVideoService.MODEL_CAPS (the legacy Grok
# default xai/grok-imagine-video/reference-to-video remains registered).
# Available video models documented at https://www.atlascloud.ai/docs/models/video
ATLAS_VIDEO_MODEL=
# Where canvas formats the Omni models can't render (anything outside
# 16:9/9:16) route automatically. Defaults to Grok Imagine 1.5.
ATLAS_VIDEO_FALLBACK_MODEL=
# Output resolution for the Gemini Omni default (720p | 1080p | 4k).
# Defaults to 720p; 1080p costs the same, 4k adds $0.80/render.
ATLAS_VIDEO_RESOLUTION=
# How many reference images ship per generation (1–7). Defaults to 3
# (primary image + first two alt views). Per-brand/per-product
# videoSettings.referenceImageCount overrides this env.
ATLAS_REFERENCE_IMAGE_COUNT=

# Generative reframe of video reference images — uncrop/outpaint each ref to
# the target aspect (keep whole product) and store on Media.metadata.reframes
# for reuse. Master switch; set false to fall back to Cloudinary crop only.
REFRAME_ENABLED=true
# Atlas image-edit model for uncrop outpaint (billable per image, single submit).
# "-developer" is a HALF-PRICE billing variant, not a lower-fidelity tier
# (same origin list price + a 50% discount factor; identical profile/readme).
REFRAME_OUTPAINT_MODEL=google/nano-banana-2/edit-developer
# Outpaint output resolution (1k|2k|4k). Keep 1k: no Atlas model has a mask, so
# the whole canvas is re-synthesised and 4k makes the decoder invent letterforms.
# The reference feeds a 720p video render anyway — raise ATLAS_VIDEO_RESOLUTION
# instead if you want resolution where viewers see it.
REFRAME_RESOLUTION=4k
# Skip outpaint when source aspect is within this ratio of target (0–1).
REFRAME_SKIP_THRESHOLD=0.985
# Per-image outpaint price recorded in the cost ledger (observability only).
# $0.04 = -developer @ 1k. Readme prices 1k/2k/4k at $0.08/$0.12/$0.16 and says
# "4K costs 2x" — if you raise REFRAME_RESOLUTION, raise this or under-report.
REFRAME_COST_USD=0.08
# Relative ratio tolerance when validating outpaint output (0–1). Default 0.05.
REFRAME_RATIO_TOLERANCE=0.05
# Max bytes for source / outpaint downloads (default 50 MiB).
REFRAME_MAX_SOURCE_BYTES=52428800
# Product-only (flat-lay / studio) shots skip the billable outpaint and use a
# deterministic $0 Cloudinary c_pad instead. Generative outpaint FABRICATES
# MERCHANDISE on these: measured shorts -> full-length trousers, a waistband
# crop -> an invented whole garment, embroidery re-arranged. Set false only if
# you accept that risk.
REFRAME_PRODUCT_ONLY_PAD=true
# Max per-channel stddev on an extended edge to treat the background as flat
# and match it with a solid fill. Studio white measures 0.0; lifestyle 27-39.
REFRAME_BORDER_STD_MAX=8
REFRAME_PROMPT_STYLE=reframe

# Optional tuning. Defaults: 5s poll interval, 10min timeout.
ATLAS_POLL_INTERVAL_MS=
ATLAS_TIMEOUT_MS=
# Rate-limit backoff schedule (comma-separated ms), e.g. 30000,60000,120000.
# Defaults tuned for Grok's 1 RPS; Gemini Omni's Atlas limit is unpublished.
ATLAS_RATE_LIMIT_BACKOFF_MS=
# Optional override (e.g., for staging tenant). Defaults to https://api.atlascloud.ai/api/v1
ATLAS_BASE_URL=
# Claude model used by services/atlasTextService.js (brand-script
# generation, other text tasks). Defaults to anthropic/claude-sonnet-4.6.
# Bump to anthropic/claude-opus-4.7 for tasks that need more reasoning.
ATLAS_TEXT_MODEL_ID=
# Text endpoints live at /v1 (NOT /api/v1 like video). Override only
# if Atlas moves the text path or you're pointing at a staging tenant.
# Defaults to https://api.atlascloud.ai/v1
ATLAS_TEXT_BASE_URL=

# ─────────────────────────────────────────────────────────────────────
# Sales Demos — public scrape for demo Brands. Per-brand handle/URL
# (+ optional method) is stored on Brand.apifyDemo. Demo brands live
# under the auto-seeded "Sales Demos" Advertiser.
#
# Catalog default is 'shopify-direct' (no Apify) when a shopifyUrl is
# set; method 'apify' keeps the legacy shopify-scraper path. Instagram
# ingestion stays on Apify in both methods (hybrid).
# APIFY_* vars below are only required for method='apify' and for
# Instagram ingestion.
# ─────────────────────────────────────────────────────────────────────
APIFY_TOKEN=
# Actor slugs. Override if we swap to community actors.
APIFY_IG_ACTOR=apify/instagram-scraper
APIFY_SHOPIFY_ACTOR=apify/shopify-scraper
# Per-source result caps per sync run. Defaults are sweet-spot for
# demo brands (IG 50 posts, Shopify 200 products). Bump for larger
# catalogs; be aware of downstream enrichment cost (~$0.05-0.12 per
# Shopify product enrichment).
APIFY_IG_LIMIT=50
APIFY_SHOPIFY_LIMIT=200
# Max products per direct-Shopify demo sync (method='shopify-direct').
# Default 200 when unset.
SHOPIFY_DIRECT_LIMIT=

# ── Direct-Shopify access hardening (headless / bot-protected stores) ──
# The shopify-direct catalog acquisition runs a resolver ladder
# (services/shopifyAccessResolver.js): primary products.json →
# myshopify-backend discovery (for HEADLESS Hydrogen/Next/Remix stores
# whose custom domain doesn't serve products.json) → tokenless Storefront
# GraphQL → sitemap. All rungs use the shared polite client
# (services/httpScrapeClient.js): per-domain concurrency + robots.txt +
# 429/Retry-After backoff + conditional GET. Both knobs optional.
HTTP_SCRAPE_DOMAIN_CONCURRENCY=   # per-domain in-flight cap (default 3)
HTTP_SCRAPE_MIN_GAP_MS=           # min gap between requests per domain (default 250)
#
# Layer 4 — LAST-RESORT headless render (services/headlessScrapeService.js)
# for Cloudflare-hard / fully-JS storefronts. OFF by default (memory- and
# latency-heavy: launches the bundled Chrome, extracts __NEXT_DATA__ /
# Remix / Nuxt / Apollo hydration + JSON-LD). Enable only where the dyno
# has headroom.
SHOPIFY_HEADLESS_RENDER=          # 'true' to enable the render fallback (default off)
SHOPIFY_HEADLESS_TIMEOUT_MS=      # per-store hard cap (default 120000)
#
# Generic catalog scraper (services/genericCatalogResolver.js) — client-
# agnostic XML-sitemap + schema.org JSON-LD ingester for non-Shopify
# server-rendered stores. Selected per-brand via apifyDemo.method =
# 'generic-sitemap' (target URL reuses apifyDemo.shopifyUrl). Reuses the
# HTTP_SCRAPE_* throttle knobs above.
GENERIC_CATALOG_ENABLED=          # 'false' to hard-disable the method (default ON; method is per-brand opt-in)
GENERIC_CATALOG_LIMIT=            # max products per sync (default 10000 = effectively full catalog; enrichment cost scales with this)
GENERIC_CATALOG_MAX_SITEMAP_URLS= # hard ceiling on candidate URLs scanned per sync (default 20000)
GENERIC_CATALOG_PDP_CONCURRENCY=  # parallel product-page fetches during scan (default 5); auto-falls to serial when a site declares a crawl-delay. httpScrapeClient still enforces the per-host min-gap.

# Catalog per-product detect (crops / overlay safe-zones / ad-readiness).
# Deferred by default: instead of running for the whole catalog at sync
# time (expensive, mostly wasted — most products never become ads), detect
# runs ON-DEMAND at ad-generation time for the products a campaign uses
# (services/catalogProductDetectService.js ensureDetectForProducts) + a
# pre-warm when an IG post confirms a product match. Set true to restore
# eager whole-catalog precompute at sync time.
CATALOG_DETECT_PRECOMPUTE=        # 'true' = eager precompute (legacy); default false = defer to ad time

# Catalog enrichment (services/catalogProductEnrichmentService.js).
# AUTO after sync = reviews-only GAP-FILL (products with no on-page review
# signal). The paid cross-seller pricing + web-wide review synthesis is
# USER-ACTUATED via the "Enrich" button (POST /brands/:id/enrich).
CATALOG_ENRICHMENT_CONCURRENCY=   # parallel products enriched (default 6; pure API calls)
CATALOG_ENRICHMENT_MAX_PER_RUN=   # cap products enriched per run (default 500)


# Apify proxy group. Leave empty for default (datacenter). Set to
# RESIDENTIAL when a target Shopify store or IG account is firewalled
# against datacenter IPs (403s / anti-scrape blocks). Residential
# costs 5-10x more per GB — only enable when needed.
#   ''             default datacenter (cheapest)
#   RESIDENTIAL    real-user IPs (bypasses most firewalls, expensive)
#   AUTO           actor picks per hostname
APIFY_PROXY_GROUP=
# Comma-separated emails allowed to bootstrap the Sales Demos workspace
# (via /sales-demos "Set up workspace" button). After the first admin
# lands, additional sales reps get invited via the normal Team invite
# flow. Case-insensitive.
SALES_DEMOS_ADMINS=

# ── Video titling engine (Remotion) ─────────────────────────────────────
# Which compositor titles video ads when a brand has no explicit
# Brand.videoSettings.titlingEngine. Chain (most specific wins):
#   custom styleScript for the format → canvas (forced)
#   → Brand.videoSettings.titlingEngine → TITLING_ENGINE → 'remotion'
#   'remotion'  Remotion 4 SSR — spec-driven canonical compositions,
#               brand webfonts, layered shadows, spring motion (DEFAULT
#               when this env is unset)
#   'canvas'    legacy/scrim sandboxed per-frame canvas scripts —
#               reachable via custom styleScript* or explicit override
# NOTE: Remotion is COMMERCIALLY LICENSED for companies >3 people
# (remotion.pro — company license + per-render seats). Confirm licensing
# before production use at scale.
TITLING_ENGINE=remotion
# Title placement mode scan depth (content mode only). Chain for the
# effective mode: request > Brand.videoSettings.titlePlacementMode >
# default 'canonical'. TITLE_PLATE_SCAN controls plate-intel scan depth
# when mode is content; 'off' is a global kill switch forcing canonical
# (no plate scan, no nudge, no ink flip).
#TITLE_PLATE_SCAN=basic
# Remotion render tuning. Renders are memory-heavy (~1.5-3GB peak with
# headless Chrome) — the service queues renders at concurrency 1 per
# process; size the Render.com plan to >= 4GB for comfortable headroom.
# REMOTION_BROWSER_EXECUTABLE overrides browser discovery (needs a
# chrome-headless-shell binary — modern full Chrome removed old
# headless). Unset = auto-discover, else download at boot.
#REMOTION_BROWSER_EXECUTABLE=
# Frames rendered in parallel inside one render job (null = Remotion
# picks from cores). Lower to 1-2 on small instances.
#REMOTION_CONCURRENCY=
# Per-render timeout (default 180000 = 3 min; remote plate download +
# 200 frames must fit inside it).
#REMOTION_TIMEOUT_MS=180000
