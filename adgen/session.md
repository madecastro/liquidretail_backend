# session.md — liquidretail_adgen (SUPERSEDED)

**Superseded 2026-09-06.** This tree is now `adgen/` inside
`liquidretail_backend` (trunk `main`, merge `e6393912`). Live handoff is
the **root** `session.md`. Architecture: `adgen/CLAUDE.md` (header) and
root `CLAUDE.md` (monorepo rules). Narrative:
`session.d/2026-09-06_monorepo-graft-landed.md`. Snapshot:
`/Volumes/Sayulita/Projects/RS/render-deploy-snapshot-2026-09-06.md`.

Do not append CURRENT STATE here. The 2026-09-06 adgen-api SIGTERM
NEXT-SESSION below is resolved as Render's deploy-swap drain (benign;
see snapshot Stage 4).

---

## NEXT-SESSION PROMPT

_(cleared — handoff moved to the repo-root `session.md`.)_

---

## CURRENT STATE

**Superseded.** See root `session.md` CURRENT STATE: graft live, Stages
0–4 done, Stage 5 dashboard-only and pending, Stage 6 pending, autoDeploy
OFF on all six Render services, freeze still in effect.
